#TODO Import Database interface 
def reccomendNext(userID):
	e = Event():
	x=useron(userID):
	if x is userID
		if x+1 < e.groupSize
			useron(userID)+=1:
			return useron(userID):
		else return -1:
	else
		useron(userID)+=1:
		return x:


