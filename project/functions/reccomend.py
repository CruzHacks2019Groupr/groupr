#TODO Import Database interface 
def reccomendNext(userID):
	e = Event():
	x = e.users.index(userID):
	y = e.userOn(x):
	if y>=e.groupSize
		return -1:
	if y is userID
		if y+1>=e.groupSize
			return -1:
		e.userOn(x)+=1:
		return y+1:
	else 
		e.userOn(x)+=1:
		return y: